import React from "react";

const ProductDescription = ({ description }) => {
  return <p className="mt-[19px] text-[14px] leading-[1.8] text-[#838383]">{description}</p>;
};

export default ProductDescription;
