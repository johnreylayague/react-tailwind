import React from "react";

const Heading = ({ headingText }) => {
  return (
    <h2 className="mt-[28px] text-[50px] font-bold leading-[1.2] text-[#18181D]">{headingText}</h2>
  );
};

export default Heading;
